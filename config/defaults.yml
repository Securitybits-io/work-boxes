---
provider:
  memory: 4000
  cpus: 4

linux:
  locale:
    keyboard: se
    timezone: Europe/Stockholm

windows:
  locale:
    timezone: W. Europe Standard Time
    language: en-SE

engagement:
  shared_folder: Shared
  vm_group: /Pentest

machines:
  Kali:
    enabled: true
    box: kalilinux/rolling
    box_version: "2026.2.0"
    os: linux

  Windows:
    enabled: false
    box: commando/default
    os: windows
    disk_size: 80GB
